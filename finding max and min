#include <stdio.h>
int main() {
    int a[5] = {5, 3, 8, 1, 2}, i, max = a[0], min = a[0];
    for (i = 1; i < 5; i++) {
        if (a[i] > max) max = a[i];
        if (a[i] < min) min = a[i];
    }
    printf("Maximum = %d\nMinimum = %d", max, min);
    return 0;
}
